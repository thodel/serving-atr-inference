fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
loguru>=0.7
pillow>=10.0
# Kraken powers party HTR
kraken>=5.0
# `party` — mittagessen's PAge-wise Recognition of Text-y (Swin encoder + Llama
# decoder). Pinned to a commit, not a name, for two reasons:
#
#   1. The PyPI package called `party` is something else entirely — a
#      "Lightweight Python client to the Artifactory API" (checked 2026-08-11).
#      `pip install party` installs an unrelated dependency and leaves #32 open.
#   2. It has no releases; the version string is a git describe
#      (0.0.0.post492+gc2589b1), so only a commit is reproducible.
#
# It registers `PartyModel` through the `kraken.models` entry point, which is what
# makes `RecognitionTaskModel.load_model` able to read this engine's
# model.safetensors at all. Adding it changes nothing about kraken or torch —
# the install is additive (timm, pillow-jxl-plugin and transitive deps).
party @ git+https://github.com/mittagessen/party.git@c2589b1
torch>=2.2.0
torchvision>=0.17.0
