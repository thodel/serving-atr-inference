# vLLM venv. asterAIx: Python 3.12, driver 565.57.01 / CUDA 12.7.
#
# IMPORTANT: torch is installed SEPARATELY as 2.8.0+cu128 by make_venvs.sh.
# Current vLLM (0.2x) ships a CUDA-13 build of vllm._C (needs libcudart.so.13 /
# driver >=580) and fails on this driver. vLLM 0.11.0 is the last CUDA-12.8 build
# that still supports Qwen3-VL (Qwen3-VL requires vLLM >= 0.11.0); it pins
# torch==2.8.0, so the pre-installed cu128 build is preserved.
# NOTE: LightOnOCR-2 (a Jan-2026 model) may be too new for 0.11.0 and not load;
# revisit if that model is needed (would require the driver upgrade / cuda-compat).
vllm==0.11.0
# vLLM 0.11.0 uses transformers 4.x tokenizer APIs (e.g. all_special_tokens_extended,
# removed in transformers 5.x -> AttributeError). Pin a 4.x that still supports
# Qwen3-VL (added in transformers 4.57).
transformers>=4.57,<5
